# 2026-09-04 工作记录

## 微博浏览器模块升级（sync-weibo.md v1.0 → v2.0）
- 将「同步微博」发布单向模块升级为「微博浏览器」一体化设计：浏览时间线/分组、瀑布流布局、点赞/评论/转发、AI 编辑发布、表情包渲染、图片/视频下载与统一命名。
- 关键架构决策：以微博移动端**私有 API**（m.weibo.cn / api.weibo.cn，扫码/Cookie 登录）为主干，官方 Open API 降级为发布备用；私有 API 存在 ToS 合规风险，已在文档第 10 节标为最高风险并列为开放问题，需用户确认。
- 媒体下载命名规范：图片 `{mid}_{seq:02d}_{author}.{ext}`，视频 `{mid}_{author}_{quality}.{ext}`，表情包 `{name}.{ext}`；去非法字符、冲突追加 `_n`、按 url 去重。
- 数据模型扩展：`weibo_cache` / `weibo_groups` / `interaction_records` / `media_downloads`（均存 globalStorage/social.db）。
- 同步更新 README 索引与「同步微博」→「微博浏览器」小节。
- 涉及文件：`docs/modules/sync-weibo.md`、`README.md`。

## 对照官方微博 Open API（open.weibo.com，2025-05-09 版）二次校准
- 抓取官方 API 文档后校正架构：放弃此前「私有 API 为主干」假设，改为**全程基于官方 Open API（OAuth2）**，合规可控。
- 官方可用并直接落地：OAuth2 授权、home_timeline 时间线、comments/* 评论读写、emotions 官方表情、媒体直链下载（CDN，非私有端点）。
- 官方缺失、明确降级（不实现/受限）：点赞（无 like 接口）、转发写入（无 repost 接口）、图文/视频发布（仅 statuses/share 链接分享）、分组（无接口→改为本地分组）、图片/视频上传（无 upload 接口）。
- 新增第 5 节「官方 API 能力对照与缺口」矩阵，逐能力映射接□与处理策略；区分「受限目标」（第 1.3 节）。
- README 对应描述同步改为官方 API 口径，删除私有 API 表述。

## 参考猴油脚本校准媒体下载实现（weibo-image-downloader.v1.0.js）
- 用户提供参考脚本：`d:\Git\应用\Tampermonkey\weibo-image-downloader.v1.0.js`，据此校准文档媒体下载章节。
- 真实可用路径：GET `https://weibo.com/ajax/statuses/show?id={postId}` 取 JSON（官网网页 ajax 接口，与官网同源，非 open.weibo.com REST，非逆向私有协议）。
- 媒体地址解析字段：`pic_ids[]`→`ajax/common/download?pid=`；视频→`page_info.media_info.playback_list[0].play_info.url` 或 `mix_media_info.items[].data.media_info...`；转发体 `data.retweeted_status || data`；命名用 `mblogid`/`user.id`/`created_at`。
- 关键约束：下载请求必须带 `Referer: https://weibo.com`，否则 CDN 返回 403；并发 3、重试 2 次、超时 30s、按 postId 去重（史上限 50000）。
- 命名规范改为脚本验证格式：`weibo-{uid}-{YYYYMMDDHHmm}-{postId}-{seq:02d}.{ext}`（可选追加清洗后的 author），并新增第 10.1 地址解析映射表、10.3 控制参数表。
- 顶部架构说明与第 5 节结论修正：授权/浏览/评论/表情用官方 Open API；媒体下载走官网 ajax 接口（明确非逆向私有协议）；第 11 节风险新增「媒体接口依赖/Referer 约束」两项。README 下载描述同步更新。

## 新增第 10.4 节 TypeScript 提取函数
- 用户确认需要把第 10.1 JSON 解析逻辑落成可直接复用的 TypeScript 函数签名。
- 在 sync-weibo.md 第 10.4 节新增：`WeiboMediaItem`/`WeiboDetail` 接口，以及 `resolveTarget`/`extractMedia`/`formatWeiboTimestamp`/`sanitizeAuthor`/`generateMediaFilename`/`buildDownloadTasks` 函数（显式类型、无 any、早期返回、`?.` 安全取值），并标注 zod 收窄 unknown 的建议与调用顺序。

## 新增第 12 节 TypeScript 关键交互逻辑
- 用户确认将「评论读取/发表」与「AI 编辑发布的 diff 采纳」也落成函数签名。
- 在 sync-weibo.md 新增第 12 节：12.1 评论读写（`fetchComments`/`postComment` 基于官方 comments/show|create|reply，含 sha256 幂等去重、`normalizeComment` 防御性取值）；12.2 AI 编辑与 diff（`requestAIEdit`/`computeUnifiedDiff` 行级比对/`applyDiffDecision`，复用 DeepSeek）。原第 12 节「与 v1.0 差异摘要」顺延为第 13 节。

## 新建 src/modules/weibo/ 代码骨架
- 仓库此前仅有 docs/ 与 README，无源码；本次落地首个代码模块。
- 文件划分：`types.ts`（接口/常量）、`auth.ts`（TokenStore，解耦 vscode，依赖 `SecretStorageLike`）、`client.ts`（`weiboFetch` 超时+退避重试）、`comments.ts`（`fetchComments`/`postComment`/`sha256`/`normalizeComment`）、`media.ts`（`resolveTarget`/`extractMedia`/`formatWeiboTimestamp`/`sanitizeAuthor`/`generateMediaFilename`/`buildDownloadTasks`）、`ai.ts`（`setLLMClient`/`requestAIEdit`/`computeUnifiedDiff`/`applyDiffDecision`）、`index.ts`（barrel 导出）。
- 实现严格对应 docs/modules/sync-weibo.md 第 10.4 / 12.1 / 12.2 节函数签名；显式类型、无 any、早期返回。
- 待补：仓库尚无 package.json/tsconfig/@types/vscode，构建配置需后续建立；`crypto.subtle` 依赖 Node18+ 全局 crypto。

## 新增「保存为笔记」能力（B22）
- 用户需求：微博内容直接保存到插件笔记模块。
- 设计：复用 notes.md「网页剪藏」统一 Markdown 模板（Frontmatter: title/created/tags/source）；微博转笔记后与笔记模块同构，可被双链/检索/版本管理。
- docs/modules/sync-weibo.md 更新：US-9、功能清单 B22、新增 4.6 保存为笔记流程、数据模型 `weibo_saved_notes`（去重+「已保存」角标）、命令 `baiwanyione.weibo.saveToNote` 与 webview `weibo/saveToNote`、验收 AC-15、第 12.3 节 TS 实现、差异摘要补充。
- 代码：src/modules/weibo/note.ts 新增 `buildWeiboNoteMarkdown`/`saveWeiboToNote` 与 `NoteInserter` 接口（解耦笔记模块，落盘由集成层调用笔记模块 createFromContent 完成）；index.ts 已补 `export * from './note'`。

## 文件重命名：sync-weibo.md → weibo.md
- 用户要求将 docs/modules/sync-weibo.md 重命名为 weibo.md（PowerShell Rename-Item，git 内）。
- 同步更新全仓 8 处引用：README.md（2）、src/modules/weibo/types.ts、src/modules/weibo/media.ts、docs/roadmap.md（2）、docs/modules/sync-wechat.md（2）。重命名后搜索 sync-weibo 结果为 0。

## 存储架构重构与 press/notes/reader 模块目录设计落地
- 用户提出存储目录 6 项设想：①可配置文档保存目录（git 跟踪）；②模块目录 notes/press/reader；③SQLite 索引按目录+文件名；④notes 年月/UUID（32 位十六进制时间戳打散，索引 path+首行 `#` 标题）；⑤press 小说目录（README.md 背景 + `Chapter/01-章节名.md`，分卷 `Chapter/01-卷名/`）；⑥press 设定扁平独立文档（角色设定.md/大纲.md/设定.md）；⑦章节文档体含本章介绍/出场角色链接/金手指设定。
- 确认决策：索引库留 globalStorage 不进 Git（仅文档进 Git）；press 与 writing 合并为统一 press（小说写作）模块；设定文档扁平独立。
- 落地：重写 docs/modules/press.md（融合 writing 插件形态 + 旧 press 宅桌面，采用插件形态），删除 docs/modules/writing.md；common.md 新增 1.5 存储根与模块目录 + `storage.rootPath` 配置；notes.md 改年月/UUID 结构；reader.md 补模块目录存储小节；README.md 模块命名 press + 通用存储小节 + 历史参考表移除 press；roadmap.md S5 写作→小说写作 Press 并改引用；markdown-one.md 引用改 press。
- 涉及文件：press.md、writing.md(删)、common.md、notes.md、reader.md、README.md、roadmap.md、markdown-one.md（共 8 个，触发 git 提交询问）。

## 合并阅读模块文档：reading.md → reader.md
- 用户要求将 docs/modules/reading.md 与 docs/modules/reader.md 合并为 reader。
- 关系：reading.md 为插件形态重构版（权威、完整，R1~R19、数据模型、验收 AC-1~AC-12）；reader.md 为宅桌面时期 Express/Drizzle 历史实现（其自身标注迁移时按 Markdown/SQLite 重写）。
- 合并策略：以 reading.md 插件形态设计为主体，吸收 reader.md 独有的「模块目录与存储（设想②）」约定（`{root}/reader/` 目录 + `globalStorage/reader.db` + 只读原则），删除已废弃的 Express 实现细节。
- 一致性修正：reading.md 原索引库名 `reading.db` 与模块名 reader 不一致，统一为 `reader.db`（与 common.md 设想② reader 模块对应）。
- 删除 docs/modules/reading.md；README.md（2 处链接）、docs/roadmap.md（2 处引用）统一指向 reader.md。
- 涉及文件：reader.md（重写）、reading.md（删）、README.md、roadmap.md（共 4 个，未超 5 个，按规则不弹提交询问）。

## 重命名并升级公众号模块：sync-wechat.md → wechat.md（V2.0）
- 用户要求将 docs/modules/sync-wechat.md 重命名为 wechat.md，并升级为「微信公众号写作并同步发布和管理」，参考成熟应用（壹伴、新媒体管家、秀米、135、公众平台助手）完善。
- 保留 git 历史：用 `git mv` 重命名（非新建+删除）。
- 升级内容（V1.0「同步微信公众号」单向同步 → V2.0 写作/发布/管理全流程）：新增第 0 节模块目录与存储；定位改为写作+发布+管理；功能清单沿用 G1~G14 并新增 G15~G25（多图文编排、排版模板、AI 配图/标题、敏感词预检、定时群发、已发布管理、素材管理增强、自定义菜单、自动回复、粉丝管理、数据概览）；流程新增 4.3 管理流程；数据模型扩表（wechat_published/templates/schedule/menus/auto_replies/fan_tags/stats_cache）；接口命令与平台接口表扩展（menu/*、user/*、datacube/*）；验收 AC-1~AC-19；风险补充菜单/粉丝/数据权限与定时漏发。
- 自动回复规则说明：公众号无公开写接口，标注为本地规则 + 导出 JSON 供后台导入的降级方案。
- 引用同步：README（索引行+功能需求小节标题/链接）、roadmap（S0 并行任务 + S7 公众号清单）、markdown-one（2 处）、weibo（对比模块名+链接）全部指向 wechat.md；roadmap 引用的 G1~G14 编号在升级版中保留以兼容。
- 涉及文件：wechat.md（覆盖）、README.md、roadmap.md、markdown-one.md、weibo.md（共 5 个，未超 5 个，按规则不弹提交询问）。

## 根据模块文档同步更新 README.md
- 用户要求「根据文档更新 README.md」。核对各模块最新文档（reader.md v1.1 合并版、wechat.md v2.0）后，修复 README 中 3 处与模块文档不一致：
  1. 「### 微信公众号」小节：原停留在 v1.0 单向同步描述（草稿箱/素材/排版/凭证），重写为 v2.0 写作/发布/管理全流程（写作即排版、素材资产化、一键发布含定时群发、运营管理含菜单/自动回复/粉丝/数据概览、可靠性）。
  2. 「### 阅读」小节：补充合并后 reader.md v1.1 的「本地 TXT 只读阅读器」定位说明与 `globalStorage/reader.db` 存储约定（只读不修改源文件）。
  3. 「历史参考文档」表：移除重复且误标的 `reader.md` 行（其已是当前权威文档，已列于模块设计文档表），避免误导。
- 文档索引表（模块设计文档）此前已正确指向 reader.md / wechat.md，本次未改动；笔记/Press/微博/知识库/Markdown ONE/AI Chat/通用小节与对应模块文档一致，未动。
- 涉及文件：README.md（仅 1 个，未超 5 个，按规则不弹提交询问）。

## 重写开发计划 docs/development.md（V4 插件形态 4 周期）
- 用户要求基于 docs 文档重写详细开发计划到 development.md；规划 4 个周期，按模块重要性优先排序；每周期明确划分需求分析/功能开发/测试验收阶段；列出核心任务、功能模块拆分、技术实现要点、交付物；含时间安排与里程碑；预估单人+AI 辅助工时；优先级用户指定 `ai-press-notes`（其他按建议）。
- 形态对齐：旧版 development.md 为 V3 全栈浏览器应用；本版替代为 VSCode 插件形态（Markdown 主存储 + SQLite 仅缓存索引），对齐 roadmap.md 与各模块文档。
- 4 周期结构：周期一 基座+创作核心闭环（Common+Markdown-ONE+Notes+KB最小集+AI-Chat，~250h，第1-10周）；周期二 小说写作 Press（~130h，第11-17周）；周期三 阅读+全局搜索+模块联动（~90h，第18-23周）；周期四 分发+Dashboard+Extension+打磨（~135h，第24-29周）。
- 优先级排序：第一梯队 Notes/AI-Chat/Press（占~42%）；第二梯队 Common/Markdown-ONE/KB（依赖前置）；第三梯队其余按 roadmap 建议顺序。总工时 ~605h（含测试验收与约10%缓冲），约 121 有效工作日 / 29 日历周。
- 关键适配点标注：Dashboard 改为插件首页 Webview、浏览器扩展「保存为笔记」改为经插件命令写 notes/（替代 localhost:3006 后端）；微博全程官方 Open API 主干、公众号 Token singleflight 与 IP 白名单降级。
- 涉及文件：docs/development.md（仅 1 个，未超 5 个，按规则不弹提交询问）。

## 参照 shop.dev 重写 development.md 为「具体项目模块」风格
- 用户要求 development.md 需要具体的项目模块，参考 `d:\Git\Projects\shop.dev\docs\development.md`。
- 重读 shop.dev 开发计划，提炼其结构特征：按周拆分阶段、每阶段含「需求分析表（任务|内容|交付物）+ 功能开发表（线路|任务|技术要点|人·天|状态）+ 阶段交付物」、每期含测试验收表与里程碑、文末含四期对比总览/工时汇总（按线路）/AI提效分析/依赖矩阵/时间线ASCII图。
- 提取本项目全部模块具体功能点（C1~C16、M1~M12、N1~N14、K1~K12、A1~A17、W1~W26、R1~R18、B0~B22、G1~G18，Dashboard 8 组件、Extension 10 任务、O1~O9），逐项拆入各周期功能开发表。
- 定义本项目 4 条「线路」维度：EH（扩展宿主）/ WV（Webview 前端）/ DB（索引数据层）/ AI（AI 服务层），替代 shop.dev 的后端/后台/小程序。
- 工时单位统一为「人·天」（1 人·天=5h）：周期一 ~52、周期二 ~26、周期三 ~18、周期四 ~27，合计 ~123 人·天≈615h≈29 周。各具体任务分配人·天数并在表内逐项标注技术要点与线路。
- 保留 V4 优先级排序（ai-press-notes 第一梯队）与形态适配说明；状态符号统一 ⬜ 计划。
- 涉及文件：docs/development.md（仅 1 个，未超 5 个，按规则不弹提交询问）。
