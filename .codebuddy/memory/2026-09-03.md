# 2026-09-03 工作记录

## 完善 README.md 功能需求

- 依据 `docs/prd.md`、`docs/development.md`、`docs/optimization.md`、`docs/modules/*.md`、`docs/plan/SQLite 仅做缓存 Markdown 做主存储.md`，补全 `README.md` 的「功能需求」各模块条目。
- 涉及文件：`README.md`
- 结构要点：技术栈补充 VSCode Extension API 与 DeepSeek SSE；新增「文档索引」表；通用（Markdown 主存储 + SQLite 缓存索引、FTS5/全局搜索、SecretStorage 密钥、命令面板、回收站）、AI Chat（写作/对话双模式、diff 接受、@上下文提及、上下文参数）、笔记、写作、阅读、同步微博、同步微信公众号、知识库、Markdown ONE。
- 微博 / 公众号两模块 docs 未覆盖，已在 README 中显式标注「docs 尚未覆盖，以下为初步规划」并附幂等与退避重试约束。

## 按功能模块拆分独立设计文档

- 依据 README 的 9 个功能模块，在 `docs/modules/` 下新建独立文档：`common.md`、`ai-chat.md`、`notes.md`、`writing.md`、`reading.md`、`knowledge-base.md`、`markdown-one.md`、`sync-weibo.md`、`sync-wechat.md`；旧文档（press.md、reader.md、dashboard.md、extension.md）保留为「宅桌面时期」历史参考，不覆盖、不删除。
- 涉及文件：上述 9 个新文档 + `README.md`（文档索引与模块链接）。
- 统一 9 节文档模板：概述（定位/目标/非目标）→ 用户场景 → 功能清单（P0–P2 + 状态）→ 交互与流程 → 关键技术选型对比 → 数据模型 → 命令与消息协议 → 验收标准 → 风险与开放问题。
- 全局技术约定（后续各模块文档与实现须一致）：Markdown 主存储 + SQLite 仅存元数据（不存正文全文）；索引库放 `globalStorage` 不参与 Git；密钥一律 `SecretStorage`；Webview 用 `reqId` 请求-响应 + `stream` 增量协议；一切文件写操作走 `WorkspaceEdit` 保证可撤销；网络失败一律指数退避 + 抖动 + 超时上限；发布类操作本地内容 hash 做幂等；检索基线为 FTS5 trigram，向量检索为可选增强且默认关闭。
- README 文档索引已拆为「模块设计文档（插件形态）」与「历史参考文档（宅桌面时期）」两组，各功能小节标题下附对应设计文档链接。

## 新增统一开发路线图 docs/roadmap.md

- 依据 9 篇模块文档的依赖关系排出 S0~S8 九个阶段：地基 → Markdown 管线 → 笔记 → 知识库 → AI Chat → 写作 → 阅读 → 分发 → 打磨，合计 ~540h ≈ 22 周；另给出 ~215h ≈ 9 周的 MVP 快速通道（通用核心 + 管线最小集 + 笔记 + 知识库最小集 + AI Chat）。
- 涉及文件：`docs/roadmap.md`（新增）、`README.md`（文档索引加「排期」分组）。
- 排期原则（后续调整路线图须遵守）：地基优先；尽早打通「笔记 + AI」最小闭环；外部接口权限风险前置到 S0 并行调研；每阶段必须产出可独立交付的增量。
- 各阶段只引用模块文档的功能编号与验收标准编号，不在路线图中重复列举，避免两处不一致；新需求须先回填模块文档再排期。
