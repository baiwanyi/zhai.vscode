# 2026-09-14 工作记录

## webview 独立预览开发能力
- 用户需求：只进行页面构建并在浏览器中预览开发，不启动扩展宿主调试。
- 现状：项目已重构为 shadcn/ui + React 组件化（src/components/ui、src/hooks、src/lib）；Vite 配置统一为根目录 `vite.config.mjs`（root: src/webview，base: './'，resolve.alias '@' -> src，build.outDir: dist/webview）。package.json scripts 用 `vite build` / `vite build --watch`（无 --config，Vite 自动识别 vite.config.mjs）。
- 改动：`package.json` 新增 `webview:serve` 脚本（`vite`，即 Vite 8 dev server + HMR）。此前 `src/webview/bridge.ts` 已对 `acquireVsCodeApi` 做浏览器安全降级（顶层 typeof 检测，浏览器中为 null，宿主侧行为不变），是浏览器预览不白屏的前提。
- 用法：`pnpm run webview:serve` 后访问 http://localhost:5173 即可实时预览 webview 页面；仅 UI 预览，宿主 request 在浏览器中不可用（直接 reject）。
- 验证：`pnpm exec tsc --noEmit` 与 `pnpm exec vite build` 均通过。

## 项目启用 ESLint（用户要求）
- 形态：ESLint 9.39.5 扁平配置（根目录 eslint.config.mjs）+ typescript-eslint 8.70（类型感知）+ eslint-plugin-import-x + eslint-plugin-react + react-hooks + eslint-config-prettier。
- 关键阻碍与解法：TypeScript 7.0 不提供程序化 API，typescript-eslint 检测到 TS major ≥ 7 会直接抛错。按官方 TS 7.0 发布说明用 npm 别名并行：`typescript: npm:@typescript/typescript6@^6.0.2`（提供 tsc6 与 TS 6 API），另加别名 `@typescript/native: npm:typescript@^7.0.2` 保留 TS 7 的 tsc。实测 tsc=7.0.2、tsc6=6.0.3，typecheck 正常。
- 版本约束：eslint-plugin-react 7.37.5 仅声明支持到 ESLint 9.7（ESLint 10 移除了 context.getFilename，插件会崩），因此 ESLint 与 @eslint/js 锁 9.39.5；eslint-plugin-react-hooks 7.1.1 已支持 ESLint 10。
- 规则要点：全局 `linterOptions.noInlineConfig`（落地「禁止 eslint-disable 注释」约定）；import-x/order 按 builtin → external → internal → sibling/index → type、组内字母升序、组间无空行；no-explicit-any / consistent-type-imports / explicit-function-return-type 为 error；react/prop-types 关闭（由类型系统承担）；react-hooks 只启用经典两条（v7 其余 15 条属 React Compiler 语义）。
- 托管源码例外：`src/components/ui` 与 `src/hooks` 关闭显式返回类型、no-explicit-any、import 排序及上游 any 收窄类规则；`**/*.mjs` 构建脚本整体排除（未纳入 tsconfig 项目）。
- 代码修复：weibo/media.ts 补箭头函数返回类型并去掉无 await 的 async；weibo/comments.ts 新增局部 toText 做类型收窄替代裸 String()（防 [object Object] 入库）；SearchPanel.tsx 注释掉随实现搁置的 import 与类型；其余 import 顺序与冗余类型断言由 `eslint . --fix` 自动修正。
- 新增脚本：`lint`、`lint:fix`。验证：eslint 0 问题、typecheck 0 错误、webview 构建通过。
- 环境坑：`pnpm add` 触发未授权构建时会自动往 pnpm-workspace.yaml 写入 `unrs-resolver: set this to true or false` 占位行，与手写条目并存会报 duplicate mapping key，需删掉占位行并保留 `unrs-resolver: true`。

## 构建与类型检查的 TS 版本分工（核查结论）
- `pnpm run typecheck`（`tsc --noEmit`）跑的是 TS 7.0.2：`node_modules/.bin/tsc` 的 shim 指向 `typescript@7.0.2`（由别名包 `@typescript/native` 提供），实测 `--version` 输出 7.0.2。
- `pnpm run compile` 不调用 tsc：扩展宿主由 esbuild 打包（自带 TS 解析器，仅擦除类型）、webview 由 Vite 8 打包（esbuild/rolldown 转译），两者都不做类型检查，产物构建与 TS 版本无关。
- 编辑器语言服务使用 `typescript` 别名指向的 TS 6 API 包（npm 包版本 6.0.2）；`tsc6` 可手动调用（`tsc6 --version` 输出的 6.0.3 是编译器内部构建号，与 npm 包版本不同）。

## TS 版本分工固化（用户要求）
- `compile` 脚本改为 `pnpm run typecheck && node esbuild.mjs && pnpm run webview:build`：编译前用 TS 7（tsc bin 来自别名包 @typescript/native）做类型检查，类型错误会阻断构建；`package`（vsce）沿用 compile，故同样受检。
- lint 链路继续使用 TS 6 API（typescript-eslint 依赖的 `typescript` 别名指向 @typescript/typescript6），两条链路互不干扰。
- `watch` / `watch:all` 未加入类型检查（开发模式避免反复全量检查）；如需实时类型反馈，应另起 `tsc --noEmit --watch` 进程。
- 版本核对：typescript 7.0.2 与 @typescript/typescript6 6.0.2 均为 npm latest，无需升级。
- `watch:all` 加入第三个并发进程：`tsc --noEmit --watch`（TS 7），三路输出前缀 host / web / ts（blue/green/yellow）；已实测 TS 7 监听模式可正常启动并进入 Watching for file changes。`watch`（仅宿主）保持不含类型检查。

## ESLint 规则扩充（用户提供参考配置）
- 改写为 `defineConfig` + `globalIgnores` 写法；并入规则：`@typescript-eslint/naming-convention` 完整集（变量 camelCase/PascalCase/UPPER_CASE、布尔量 is/should/has/can/did/will 前缀、typeLike 与 interface 用 PascalCase 且禁 I 前缀、enumMember 用 UPPER_CASE、memberLike 允许 snake_case、objectLiteralProperty 放宽；`**/*.d.ts` 按 typeProperty 放宽）；`import-x/first`、`import-x/no-duplicates`、`import-x/newline-after-import`；`import-x/order` 增加 `distinctGroup: false` 与 CSS 文件组（`*.{css,scss,sass,less}` 排到末尾）；`react-refresh/only-export-components`（allowConstantExport，新增依赖 eslint-plugin-react-refresh 0.5.6）；显式关闭 `react-hooks/preserve-manual-memoization`；`ecmaVersion: 'latest'`；`projectService.allowDefaultProject: ['*.mjs']` 取代原先忽略 .mjs 的做法（构建脚本也纳入 lint），并对 `**/*.mjs` 关闭类型感知规则。
- 未采用两项：`semi: ['error','never']`（格式已由 Prettier 统一，eslint-config-prettier 会关闭格式类规则，写了也不生效）；`reactHooks.configs.flat.recommended`（v7 该预设仍是 eslintrc 形态、plugins 为数组，直接 extends 会报配置格式错误，故手动启用经典两条）。
- 连带代码修复（布尔前缀规范）：fileMeta.ts `inTags` → `isParsingTags`；schema.ts `created` → `isCreated`（含 return 处）；weibo/media.ts `readable` → `isReadable`（属性与参数）。
- 附带：对 src 全量执行一次 `prettier --write`，此前 11 个未格式化文件统一（含 index.css 的 import 引号转单引号、文件末尾补换行），现在 `prettier --check src` 全绿。
- 验证：eslint 0 问题、typecheck 0 错误、`pnpm run compile` 通过、prettier 全绿。
- 已为 vendoredUi 豁免块（src/components/ui、src/hooks）的每条规则补充关闭理由注释，含七条 no-unsafe 类规则各自拦截的场景说明；注释后 prettier 与 lint 复验通过。

## 声明文件迁移 + lint 环境变化
- 按要求把 `src/webview/vite-env.d.ts` 迁移到 `src/types/vite-env.d.ts`。依据：tsconfig 的 `include: ['src/**/*.ts']` 本身即覆盖 `.d.ts`（已用 `tsc --listFiles` 验证该文件在编译列表内），故迁移无需改配置；迁移后 typecheck 通过，证明 `vite/client` 的全局声明仍生效；ESLint 的 `**/*.d.ts` 命名放宽块按 glob 匹配、与路径无关。
- 发现仓库存在其他会话/并行改动产生的提交：`cf9a740 docs(eslint): 为 vendoredUi 豁免规则补充注释`（提交的是我补的注释）、`da4b014 feat(webview): 两个侧栏视图均改用 Vite 多入口构建`；eslint 与 @eslint/js 也在该过程中被升回 `^10.10.0` / `^10.0.1`（此前为 9.39.5）。
- 后果：`pnpm run lint` 现在崩溃——`eslint-plugin-react@7.37.5` 不兼容 ESLint 10（`contextOrFilename.getFilename is not a function`，其 peer 仅声明到 ^9.7）。绕过方式：直接调用 `.pnpm/eslint@9.39.5_jiti@2.7.0_supports-color@10.2.2/node_modules/eslint/bin/eslint.js`；此时会多出 2 条来自 @eslint/js 10 recommended 的新规则告警（含 weibo/media.ts 的 no-useless-assignment）。
- 待用户决策：回退 ESLint 到 9（恢复 react 插件可用）或保留 10 并移除 eslint-plugin-react。

## 决策落地：保留 ESLint 10、移除 eslint-plugin-react（用户选方案 B）
- 卸载 `eslint-plugin-react`（其 peer 仅声明到 `^9.7`，在 ESLint 10 下加载规则即崩溃）；配置中同步移除其 import、`plugins.react`、`settings.react` 与 `react/prop-types`；保留 `react-hooks`（支持 ^3–^10）与 `react-refresh`（^9 || ^10）。
- 配置头部新增「取舍说明」：注明不引入 react 插件的原因，组件侧由 react-hooks（hooks 调用约束）与 react-refresh（HMR 边界）承担；后续若需 JSX 规则（如 jsx-key），需等 plugin-react 支持 ESLint 10 或改用其他插件。
- 结果：`pnpm run lint` 在 ESLint 10 下恢复可用（0 问题），typecheck 通过。
- 随之修复两处：`src/modules/common/searchPanelProvider.ts` 的 import-x/order（`eslint --fix` 自动处理）；`src/modules/weibo/media.ts` 的 `no-useless-assignment`（`@eslint/js` 10 新增 recommended 规则，末尾 `seq++` 的自增结果无人使用，改为直接传 `seq`）。
- 另：另一会话新增的 `src/webview/bootstrap.tsx`（多入口构建）顺带执行一次 prettier，`prettier --check src` 复归全绿。

## JSX 规则补齐：改用 @eslint-react/eslint-plugin（用户确认后实施）
- 背景：方案 B 移除 eslint-plugin-react 后缺失 JSX 规则，评估替代方案后引入 `@eslint-react/eslint-plugin@5.19.0`（peer 声明为 `eslint: *` / `typescript: *`，实测在 ESLint 10 + TS 6 API 下正常工作）。
- 配置：对 `**/*.tsx` 且 `ignores: vendoredUi` 应用 `reactX.configs['recommended-type-checked']`，并叠 `reactX.configs['disable-conflict-eslint-plugin-react-hooks']` 关闭与现有 hooks 插件的重叠规则，避免同一问题双重报告。
- 效果：自有代码 lint 0 问题；实测生效范围为——`bootstrap.tsx` 中 `@eslint-react/no-array-index-key` = 1、`dom-no-dangerously-set-innerhtml` = 1、`dom-no-dangerously-set-innerhtml-with-children` = 2，而 `components/ui/button.tsx` 中这些规则不存在（托管豁免生效）。
- 该插件的 `disable-conflict-*` 系列预设可在日后增减插件时复用（如再引入 react-hooks-extra 时）。
- 验证：lint 0 问题、typecheck 0 错误、`prettier --check src eslint.config.mjs` 全绿。

## 样式入口重命名：index.css → globals.css
- `src/webview/index.css` 更名 `src/webview/globals.css`（沿用 shadcn 生态的 globals 命名惯例）；同步其仅有的两处引用：`src/webview/main.tsx` 的 `import './globals.css'`、`components.json` 的 `tailwind.css` 字段（CLI 写入主题变量的目标）。
- 验证：typecheck 与 lint 均 0 问题；webview 构建产物 CSS 文件名与哈希与重命名前完全一致（index-CKZxiL8Y.css，16.57 kB），说明纯改路径无副作用；prettier 全绿；`shadcn info` 显示 tailwindCss 与解析路径均已指向 globals.css。

## 搜索面板页面替换（用户：换新页面，保留 React 链路）
- 删除旧页面组件 `src/webview/SearchPanel.tsx`（占位实现与其后被注释的草稿一并清除，需要时可从 git 历史取回）。
- 资产迁移：搜索协议类型 `SearchHit`、`IndexStatus` 迁至 `src/shared/types/search.ts`（原定义在旧组件内部）。
- `src/webview/main.tsx` 改为直接承载页面节点（`bootstrap(<p …/>)`），不再引用独立组件；`index.html`、vite 的 `index` 入口、`searchPanelProvider`、视图注册、`zhai.openSearch` 命令与 Ctrl+K 均保持不变，新页面只需替换 main.tsx 中渲染的节点。
- 未迁移的 UI 草稿留在 git 历史：`【】` 高亮渲染（HighlightedSnippet）与 `groupByModule` 分组逻辑；注意高亮必须用 React 节点渲染，宿主 snippet 严禁直接插 HTML（eslint-react 的 `dom-no-dangerously-set-innerhtml-with-children` 为 error 级会拦截）。
- 顺带：更新 tsconfig.json 中引用旧组件的注释，并用 prettier 统一其格式——Prettier 对 tsconfig.json 使用 jsonc 解析器，注释会被完整保留（`lib` 数组会被压成一行）。
- 验证：typecheck、lint、webview 构建、prettier 全部通过。

## 两个 webview 均改为 Vite 构建（用户要求）
- 背景：原先只有 `zhai.searchPanel`（全局搜索）走 Vite 构建的 React 页面；`zhai.placeholderView`（工作区）由 `PlaceholderWebviewProvider` 内联静态 HTML（`enableScripts:false`）渲染。用户要求两个 view 都用 Vite 构建。
- 改造：`vite.config.mjs` 的 `rollupOptions.input` 改为多入口对象 `{ index: src/webview/index.html, workspace: src/webview/workspace.html }`（仍 root: src/webview、base: './'、outDir: dist/webview），两个 HTML 同级故共享 `dist/webview/assets/`，引用均为 `./assets/`。
- 新增：`src/webview/workspace.html` + `src/webview/workspace.tsx`(入口) + `src/webview/Workspace.tsx`(占位组件，文案"暂无内容，敬请期待。")；`src/webview/bootstrap.tsx` 抽离主题同步(.dark 类)与 createRoot 挂载，供 main.tsx 与 workspace.tsx 共用；`src/modules/common/webviewHtml.ts` 抽离 `buildWebviewHtml(webview, extensionUri, htmlFile)`（读取 dist/webview 下 HTML、注入 CSP、把 `./assets/` 重写为 webview URI）。
- 改造 provider：`SearchPanelViewProvider.renderHtml` 与 `PlaceholderWebviewProvider` 均改用 `buildWebviewHtml`；后者改为 `enableScripts:true` + `localResourceRoots:[dist/webview]`，构造函数新增 `extensionUri` 参数，`extension.ts` 注册时传入 `context.extensionUri`。
- 验证：`pnpm run compile`（typecheck + esbuild 宿主 + vite 多入口）通过，产物含 `dist/webview/index.html` 与 `dist/webview/workspace.html`。

## 搜索视图改造为仪表盘（用户：将搜索改为仪表盘）
- 用户确认两点：①**全部改名**——视图 ID `zhai.searchPanel` → `zhai.dashboard`（标题「仪表盘」）、命令 `zhai.openSearch` → `zhai.openDashboard`（标题「打开仪表盘」）、类 `SearchPanelViewProvider` → `DashboardViewProvider`、文件 `searchPanelProvider.ts` → `dashboardProvider.ts`、`Ctrl+K` 改绑仪表盘；②首版范围 = **统计卡 + 快捷入口**，不新建数据库表。
- 新增：`src/shared/types/dashboard.ts`（`DashboardStats` / `ModuleStat` / `RecentFile` 契约）、`src/modules/common/db/statsRepository.ts`（单次扫描 `files` 表聚合文件数/字数/标签去重数/模块分布，`listRecentFiles` 取 mtime 倒序 5 条）、`src/webview/dashboard.tsx`（shadcn Card/Button/Progress，重建期间 1.5s 轮询收敛）。
- 协议变更：新增 `dashboard/stats`（聚合快照）与 `host/command`（命令白名单 `ALLOWED_HOST_COMMANDS` = `zhai.clearIndex` / `zhai.openSettings`）；保留 `index/rebuild`；移除 `search/query` 与 `index/status`。检索能力（`searchFiles`、FTS5 表、`zhai.search.limit` 配置）保留，供后续搜索功能复用。
- 连带改动：`indexRepository.safeParseTags` 改为导出（statsRepository 复用）；状态栏 command 与 tooltip 改为「点击打开仪表盘」；`extension.ts`、`commands.ts`、`package.json`、`bootstrap.tsx`/`webviewHtml.ts`/`vite.config.mjs`/`placeholderViewProvider.ts` 的「搜索面板」表述同步。
- 文档：`docs/modules/common.md` 命令表更新并加一行 `Ctrl+K` 归属说明；`docs/modules/dashboard.md` 新增第 13 节「插件内 Webview 首版实现」，用表映射 HTTP API → 协议、StatCardRow/QuickActions/模块分布落地情况，并注明 ActivityFeed/WidgetTodo/WidgetShortcut 待实现。
- 验证：`pnpm run lint` 0 问题（修掉 import-x/order 5 处、箭头函数返回类型、`busy` → `isBusy` 布尔前缀命名）；`pnpm run compile`（typecheck + esbuild + vite 多入口）通过。
- 遗留：视图 ID 已变更，用户已保存的侧栏布局位置需重新拖一次。
- 提交（用户指令「提交到git」）：`80009b0 feat(dashboard): 将全局搜索视图改造为仪表盘`（26 文件，含 git 自动识别的 3 处重命名：searchPanelProvider→dashboardProvider、index.css→globals.css、vite-env.d.ts→src/types/）；`80f89f5 chore(memory): 记录本轮仪表盘改造与项目长期约定`。工作区已干净。
- 环境坑（重要）：命令行直接传中文参数会被 PowerShell 按本地代码页解码成乱码（实测 `@('中文'...)` 抛 ParserError）。提交信息必须先用工具写入 UTF-8 文件（无 BOM）再 `git commit -F msg.txt`，提交后删除该文件。

## 工作区视图改造为 AI 对话（用户：工作区修改为aichat）
- 用户确认三点：①**全部改名 + 加快捷键**；②首版 = **对话闭环可用**（A6/A8/A9/A11、AC-6/AC-9）；③按 ai-chat.md 走 **openai SDK**（未采用原生 fetch 方案）。
- 依赖：`pnpm add openai` → 7.15.0 进 dependencies（安装约 1 分 40 秒，链接正常）。
- 改名：视图 `zhai.placeholderView`「工作区」→ `zhai.aiChat`「AI 对话」；`common/placeholderViewProvider.ts` → `modules/ai/aiChatProvider.ts`（类 `AiChatViewProvider`，viewId 常量 `zhai.aiChat`）；webview 入口 `workspace.html`/`workspace.tsx` → `aichat.html`/`aichat.tsx`；vite 多入口键 `workspace` → `aichat`。
- 新增命令：`zhai.openAiChat`（绑 `Ctrl+Shift+L`）、`zhai.ai.setApiKey`（输入框写 SecretStorage 的 `zhai.deepseek.apiKey`）、`zhai.ai.clearApiKey`。
- 新增宿主模块 `src/modules/ai/`：`db/connection.ts`（ai.db）、`db/schema.ts`（conversations/messages/ai_usage_logs 三表 + 索引 + meta）、`db/chatRepository.ts`、`client.ts`（openai SDK + `baseURL: https://api.deepseek.com` + `reasoning_content` 收窄读取 + usage 透传）、`chatService.ts`（会话编排、流式 60ms 合并、AbortController 取消、预算护栏、30s 首字超时、按字符÷1.6 估 token）、`aiChatProvider.ts`、`commands.ts`。
- 契约与协议：`src/shared/types/aiChat.ts`（AiConversation/AiMessage/AiSessionSnapshot/AiRuntimeInfo/AiSendResult）；方法 `ai/session`、`ai/newSession`、`ai/runtime`、`ai/send`、`ai/abort`、`host/command`；宿主 `stream` 推送复用了既有 `StreamMessage`（本次扩展 `channel`/`status`/`content`/`error` 字段）。
- 前端：`src/webview/ai-chat.tsx`（消息气泡、Enter 发送 / Shift+Enter 换行、停止、新建会话、无 Key 引导卡、按内容长度自动滚底）；`bridge.ts` 新增 `onStream` / `onState` 订阅分发（原先只处理 response）。
- 顺带重构：新增 `common/db/openDatabase.ts`（`openSqliteDatabase(uri, fileName)` 基座，index.db 与 ai.db 共用 PRAGMA；`connection.ts` 改为委托）；新增 `common/webviewCommands.ts` 共享命令白名单（`WEBVIEW_ALLOWED_COMMANDS`，dashboardProvider 改为引用它，删掉本地 Set）。
- 文档：`docs/modules/ai-chat.md` 状态改「对话模式已落地」+ 功能表 A6/A8/A9/A11 标已落地（A7/A10 部分）+ 新增第 10 节（协议表、实现位置、与设计文档的差异如 tiktoken 未接入、estimated_cost 记 0、重试未实现）；`docs/modules/common.md` 命令表新增 3 条。
- 验证：typecheck 0 错误、`eslint src` 0 问题（修掉 `streamingMessageId`/`scrollAnchor` 两个 ref 命名警告 → 加 `Ref` 后缀）、`pnpm run compile` 通过，产物含 `dist/webview/index.html` 与 `aichat.html`。
- 未提交（沿用「不擅自提交」约定）；未改动 `docs/development.md` 的计划状态列。
- 用户自测前置条件：需先运行「设置 DeepSeek API Key」命令写入密钥，才能实际对话。

## AI 对话页改用 message-scroller 组件（用户：使用message-scroller组件进行构造）
- 组件来源：`@shadcn/react` 的 `message-scroller`（`src/components/ui/message-scroller.tsx` 为其封装），配套 `message.tsx`（Message/MessageAvatar/MessageHeader/MessageContent/MessageFooter）、`bubble.tsx`（Bubble/BubbleGroup/BubbleContent/BubbleReactions）。
- 关键 API：`MessageScroller.Provider`（`autoScroll`、`defaultScrollPosition: 'start' | 'end' | 'last-anchor'`、`scrollEdgeThreshold`、`scrollPreviousItemPeek`、`scrollMargin`）、`Root`/`Viewport`/`Content`/`Item`（`messageId`、`scrollAnchor`）/`Button`（默认自带 lucide ArrowDown 与自动显隐），hooks `useMessageScroller`（scrollToEnd/scrollToMessage/scrollToStart）、`useMessageScrollerScrollable`、`useMessageScrollerVisibility`。
- 重构 `src/webview/ai-chat.tsx`：删掉 `ScrollArea` 与手写 `scrollTop = scrollHeight` 滚底逻辑，改为 `Provider(defaultScrollPosition="end")` + `Viewport` + `Content` + 每个消息一个 `Item(messageId, scrollAnchor=末条)` + `Button()`；气泡由自绘 div 改为 `Message align` + `MessageAvatar/Header/Content/Footer` + `Bubble(variant: default|muted)` + `BubbleContent(whitespace-pre-wrap)`；对齐由 `align` 联动（`data-[align=end]:flex-row-reverse`、Bubble 的 `group-data-[align=end]/message:self-end`），无需手写 justify。
- 注意：`message-scroller.tsx` 头注释明确「滚动位置由组件内部托管，调用方不要同时操作容器的 scrollTop」，故不要再恢复手动滚底。
- 验证：typecheck 0 错误、`eslint src` 0 问题、`vite build` 通过（aichat 分包由 25.17 kB 增至 33.05 kB，因引入这三组组件）。仍未提交。
