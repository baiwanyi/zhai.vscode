# 2026-09-14 工作记录

## webview 独立预览开发能力
- 用户需求：只进行页面构建并在浏览器中预览开发，不启动扩展宿主调试。
- 现状：项目已重构为 shadcn/ui + React 组件化（src/components/ui、src/hooks、src/lib）；Vite 配置统一为根目录 `vite.config.mjs`（root: src/webview，base: './'，resolve.alias '@' -> src，build.outDir: dist/webview）。package.json scripts 用 `vite build` / `vite build --watch`（无 --config，Vite 自动识别 vite.config.mjs）。
- 改动：`package.json` 新增 `webview:serve` 脚本（`vite`，即 Vite 8 dev server + HMR）。此前 `src/webview/bridge.ts` 已对 `acquireVsCodeApi` 做浏览器安全降级（顶层 typeof 检测，浏览器中为 null，宿主侧行为不变），是浏览器预览不白屏的前提。
- 用法：`pnpm run webview:serve` 后访问 http://localhost:5173 即可实时预览 webview 页面；仅 UI 预览，宿主 request 在浏览器中不可用（直接 reject）。
- 验证：`pnpm exec tsc --noEmit` 与 `pnpm exec vite build` 均通过。

## 项目启用 ESLint（用户要求）
- 形态：ESLint 9.39.5 扁平配置（根目录 eslint.config.mjs）+ typescript-eslint 8.70（类型感知）+ eslint-plugin-import-x + eslint-plugin-react + react-hooks + eslint-config-prettier。
- 关键阻碍与解法：TypeScript 7.0 不提供程序化 API，typescript-eslint 检测到 TS major ≥ 7 会直接抛错。按官方 TS 7.0 发布说明用 npm 别名并行：`typescript: npm:@typescript/typescript6@^6.0.2`（提供 tsc6 与 TS 6 API），另加别名 `@typescript/native: npm:typescript@^7.0.2` 保留 TS 7 的 tsc。实测 tsc=7.0.2、tsc6=6.0.3，typecheck 正常。
- 版本约束：eslint-plugin-react 7.37.5 仅声明支持到 ESLint 9.7（ESLint 10 移除了 context.getFilename，插件会崩），因此 ESLint 与 @eslint/js 锁 9.39.5；eslint-plugin-react-hooks 7.1.1 已支持 ESLint 10。
- 规则要点：全局 `linterOptions.noInlineConfig`（落地「禁止 eslint-disable 注释」约定）；import-x/order 按 builtin → external → internal → sibling/index → type、组内字母升序、组间无空行；no-explicit-any / consistent-type-imports / explicit-function-return-type 为 error；react/prop-types 关闭（由类型系统承担）；react-hooks 只启用经典两条（v7 其余 15 条属 React Compiler 语义）。
- 托管源码例外：`src/components/ui` 与 `src/hooks` 关闭显式返回类型、no-explicit-any、import 排序及上游 any 收窄类规则；`**/*.mjs` 构建脚本整体排除（未纳入 tsconfig 项目）。
- 代码修复：weibo/media.ts 补箭头函数返回类型并去掉无 await 的 async；weibo/comments.ts 新增局部 toText 做类型收窄替代裸 String()（防 [object Object] 入库）；SearchPanel.tsx 注释掉随实现搁置的 import 与类型；其余 import 顺序与冗余类型断言由 `eslint . --fix` 自动修正。
- 新增脚本：`lint`、`lint:fix`。验证：eslint 0 问题、typecheck 0 错误、webview 构建通过。
- 环境坑：`pnpm add` 触发未授权构建时会自动往 pnpm-workspace.yaml 写入 `unrs-resolver: set this to true or false` 占位行，与手写条目并存会报 duplicate mapping key，需删掉占位行并保留 `unrs-resolver: true`。

## 构建与类型检查的 TS 版本分工（核查结论）
- `pnpm run typecheck`（`tsc --noEmit`）跑的是 TS 7.0.2：`node_modules/.bin/tsc` 的 shim 指向 `typescript@7.0.2`（由别名包 `@typescript/native` 提供），实测 `--version` 输出 7.0.2。
- `pnpm run compile` 不调用 tsc：扩展宿主由 esbuild 打包（自带 TS 解析器，仅擦除类型）、webview 由 Vite 8 打包（esbuild/rolldown 转译），两者都不做类型检查，产物构建与 TS 版本无关。
- 编辑器语言服务使用 `typescript` 别名指向的 TS 6 API 包（npm 包版本 6.0.2）；`tsc6` 可手动调用（`tsc6 --version` 输出的 6.0.3 是编译器内部构建号，与 npm 包版本不同）。

## TS 版本分工固化（用户要求）
- `compile` 脚本改为 `pnpm run typecheck && node esbuild.mjs && pnpm run webview:build`：编译前用 TS 7（tsc bin 来自别名包 @typescript/native）做类型检查，类型错误会阻断构建；`package`（vsce）沿用 compile，故同样受检。
- lint 链路继续使用 TS 6 API（typescript-eslint 依赖的 `typescript` 别名指向 @typescript/typescript6），两条链路互不干扰。
- `watch` / `watch:all` 未加入类型检查（开发模式避免反复全量检查）；如需实时类型反馈，应另起 `tsc --noEmit --watch` 进程。
- 版本核对：typescript 7.0.2 与 @typescript/typescript6 6.0.2 均为 npm latest，无需升级。
- `watch:all` 加入第三个并发进程：`tsc --noEmit --watch`（TS 7），三路输出前缀 host / web / ts（blue/green/yellow）；已实测 TS 7 监听模式可正常启动并进入 Watching for file changes。`watch`（仅宿主）保持不含类型检查。

## ESLint 规则扩充（用户提供参考配置）
- 改写为 `defineConfig` + `globalIgnores` 写法；并入规则：`@typescript-eslint/naming-convention` 完整集（变量 camelCase/PascalCase/UPPER_CASE、布尔量 is/should/has/can/did/will 前缀、typeLike 与 interface 用 PascalCase 且禁 I 前缀、enumMember 用 UPPER_CASE、memberLike 允许 snake_case、objectLiteralProperty 放宽；`**/*.d.ts` 按 typeProperty 放宽）；`import-x/first`、`import-x/no-duplicates`、`import-x/newline-after-import`；`import-x/order` 增加 `distinctGroup: false` 与 CSS 文件组（`*.{css,scss,sass,less}` 排到末尾）；`react-refresh/only-export-components`（allowConstantExport，新增依赖 eslint-plugin-react-refresh 0.5.6）；显式关闭 `react-hooks/preserve-manual-memoization`；`ecmaVersion: 'latest'`；`projectService.allowDefaultProject: ['*.mjs']` 取代原先忽略 .mjs 的做法（构建脚本也纳入 lint），并对 `**/*.mjs` 关闭类型感知规则。
- 未采用两项：`semi: ['error','never']`（格式已由 Prettier 统一，eslint-config-prettier 会关闭格式类规则，写了也不生效）；`reactHooks.configs.flat.recommended`（v7 该预设仍是 eslintrc 形态、plugins 为数组，直接 extends 会报配置格式错误，故手动启用经典两条）。
- 连带代码修复（布尔前缀规范）：fileMeta.ts `inTags` → `isParsingTags`；schema.ts `created` → `isCreated`（含 return 处）；weibo/media.ts `readable` → `isReadable`（属性与参数）。
- 附带：对 src 全量执行一次 `prettier --write`，此前 11 个未格式化文件统一（含 index.css 的 import 引号转单引号、文件末尾补换行），现在 `prettier --check src` 全绿。
- 验证：eslint 0 问题、typecheck 0 错误、`pnpm run compile` 通过、prettier 全绿。
- 已为 vendoredUi 豁免块（src/components/ui、src/hooks）的每条规则补充关闭理由注释，含七条 no-unsafe 类规则各自拦截的场景说明；注释后 prettier 与 lint 复验通过。

## 声明文件迁移 + lint 环境变化
- 按要求把 `src/webview/vite-env.d.ts` 迁移到 `src/types/vite-env.d.ts`。依据：tsconfig 的 `include: ['src/**/*.ts']` 本身即覆盖 `.d.ts`（已用 `tsc --listFiles` 验证该文件在编译列表内），故迁移无需改配置；迁移后 typecheck 通过，证明 `vite/client` 的全局声明仍生效；ESLint 的 `**/*.d.ts` 命名放宽块按 glob 匹配、与路径无关。
- 发现仓库存在其他会话/并行改动产生的提交：`cf9a740 docs(eslint): 为 vendoredUi 豁免规则补充注释`（提交的是我补的注释）、`da4b014 feat(webview): 两个侧栏视图均改用 Vite 多入口构建`；eslint 与 @eslint/js 也在该过程中被升回 `^10.10.0` / `^10.0.1`（此前为 9.39.5）。
- 后果：`pnpm run lint` 现在崩溃——`eslint-plugin-react@7.37.5` 不兼容 ESLint 10（`contextOrFilename.getFilename is not a function`，其 peer 仅声明到 ^9.7）。绕过方式：直接调用 `.pnpm/eslint@9.39.5_jiti@2.7.0_supports-color@10.2.2/node_modules/eslint/bin/eslint.js`；此时会多出 2 条来自 @eslint/js 10 recommended 的新规则告警（含 weibo/media.ts 的 no-useless-assignment）。
- 待用户决策：回退 ESLint 到 9（恢复 react 插件可用）或保留 10 并移除 eslint-plugin-react。

## 决策落地：保留 ESLint 10、移除 eslint-plugin-react（用户选方案 B）
- 卸载 `eslint-plugin-react`（其 peer 仅声明到 `^9.7`，在 ESLint 10 下加载规则即崩溃）；配置中同步移除其 import、`plugins.react`、`settings.react` 与 `react/prop-types`；保留 `react-hooks`（支持 ^3–^10）与 `react-refresh`（^9 || ^10）。
- 配置头部新增「取舍说明」：注明不引入 react 插件的原因，组件侧由 react-hooks（hooks 调用约束）与 react-refresh（HMR 边界）承担；后续若需 JSX 规则（如 jsx-key），需等 plugin-react 支持 ESLint 10 或改用其他插件。
- 结果：`pnpm run lint` 在 ESLint 10 下恢复可用（0 问题），typecheck 通过。
- 随之修复两处：`src/modules/common/searchPanelProvider.ts` 的 import-x/order（`eslint --fix` 自动处理）；`src/modules/weibo/media.ts` 的 `no-useless-assignment`（`@eslint/js` 10 新增 recommended 规则，末尾 `seq++` 的自增结果无人使用，改为直接传 `seq`）。
- 另：另一会话新增的 `src/webview/bootstrap.tsx`（多入口构建）顺带执行一次 prettier，`prettier --check src` 复归全绿。

## JSX 规则补齐：改用 @eslint-react/eslint-plugin（用户确认后实施）
- 背景：方案 B 移除 eslint-plugin-react 后缺失 JSX 规则，评估替代方案后引入 `@eslint-react/eslint-plugin@5.19.0`（peer 声明为 `eslint: *` / `typescript: *`，实测在 ESLint 10 + TS 6 API 下正常工作）。
- 配置：对 `**/*.tsx` 且 `ignores: vendoredUi` 应用 `reactX.configs['recommended-type-checked']`，并叠 `reactX.configs['disable-conflict-eslint-plugin-react-hooks']` 关闭与现有 hooks 插件的重叠规则，避免同一问题双重报告。
- 效果：自有代码 lint 0 问题；实测生效范围为——`bootstrap.tsx` 中 `@eslint-react/no-array-index-key` = 1、`dom-no-dangerously-set-innerhtml` = 1、`dom-no-dangerously-set-innerhtml-with-children` = 2，而 `components/ui/button.tsx` 中这些规则不存在（托管豁免生效）。
- 该插件的 `disable-conflict-*` 系列预设可在日后增减插件时复用（如再引入 react-hooks-extra 时）。
- 验证：lint 0 问题、typecheck 0 错误、`prettier --check src eslint.config.mjs` 全绿。

## 样式入口重命名：index.css → globals.css
- `src/webview/index.css` 更名 `src/webview/globals.css`（沿用 shadcn 生态的 globals 命名惯例）；同步其仅有的两处引用：`src/webview/main.tsx` 的 `import './globals.css'`、`components.json` 的 `tailwind.css` 字段（CLI 写入主题变量的目标）。
- 验证：typecheck 与 lint 均 0 问题；webview 构建产物 CSS 文件名与哈希与重命名前完全一致（index-CKZxiL8Y.css，16.57 kB），说明纯改路径无副作用；prettier 全绿；`shadcn info` 显示 tailwindCss 与解析路径均已指向 globals.css。

## 搜索面板页面替换（用户：换新页面，保留 React 链路）
- 删除旧页面组件 `src/webview/SearchPanel.tsx`（占位实现与其后被注释的草稿一并清除，需要时可从 git 历史取回）。
- 资产迁移：搜索协议类型 `SearchHit`、`IndexStatus` 迁至 `src/shared/types/search.ts`（原定义在旧组件内部）。
- `src/webview/main.tsx` 改为直接承载页面节点（`bootstrap(<p …/>)`），不再引用独立组件；`index.html`、vite 的 `index` 入口、`searchPanelProvider`、视图注册、`zhai.openSearch` 命令与 Ctrl+K 均保持不变，新页面只需替换 main.tsx 中渲染的节点。
- 未迁移的 UI 草稿留在 git 历史：`【】` 高亮渲染（HighlightedSnippet）与 `groupByModule` 分组逻辑；注意高亮必须用 React 节点渲染，宿主 snippet 严禁直接插 HTML（eslint-react 的 `dom-no-dangerously-set-innerhtml-with-children` 为 error 级会拦截）。
- 顺带：更新 tsconfig.json 中引用旧组件的注释，并用 prettier 统一其格式——Prettier 对 tsconfig.json 使用 jsonc 解析器，注释会被完整保留（`lib` 数组会被压成一行）。
- 验证：typecheck、lint、webview 构建、prettier 全部通过。

## 两个 webview 均改为 Vite 构建（用户要求）
- 背景：原先只有 `zhai.searchPanel`（全局搜索）走 Vite 构建的 React 页面；`zhai.placeholderView`（工作区）由 `PlaceholderWebviewProvider` 内联静态 HTML（`enableScripts:false`）渲染。用户要求两个 view 都用 Vite 构建。
- 改造：`vite.config.mjs` 的 `rollupOptions.input` 改为多入口对象 `{ index: src/webview/index.html, workspace: src/webview/workspace.html }`（仍 root: src/webview、base: './'、outDir: dist/webview），两个 HTML 同级故共享 `dist/webview/assets/`，引用均为 `./assets/`。
- 新增：`src/webview/workspace.html` + `src/webview/workspace.tsx`(入口) + `src/webview/Workspace.tsx`(占位组件，文案"暂无内容，敬请期待。")；`src/webview/bootstrap.tsx` 抽离主题同步(.dark 类)与 createRoot 挂载，供 main.tsx 与 workspace.tsx 共用；`src/modules/common/webviewHtml.ts` 抽离 `buildWebviewHtml(webview, extensionUri, htmlFile)`（读取 dist/webview 下 HTML、注入 CSP、把 `./assets/` 重写为 webview URI）。
- 改造 provider：`SearchPanelViewProvider.renderHtml` 与 `PlaceholderWebviewProvider` 均改用 `buildWebviewHtml`；后者改为 `enableScripts:true` + `localResourceRoots:[dist/webview]`，构造函数新增 `extensionUri` 参数，`extension.ts` 注册时传入 `context.extensionUri`。
- 验证：`pnpm run compile`（typecheck + esbuild 宿主 + vite 多入口）通过，产物含 `dist/webview/index.html` 与 `dist/webview/workspace.html`。

## 搜索视图改造为仪表盘（用户：将搜索改为仪表盘）
- 用户确认两点：①**全部改名**——视图 ID `zhai.searchPanel` → `zhai.dashboard`（标题「仪表盘」）、命令 `zhai.openSearch` → `zhai.openDashboard`（标题「打开仪表盘」）、类 `SearchPanelViewProvider` → `DashboardViewProvider`、文件 `searchPanelProvider.ts` → `dashboardProvider.ts`、`Ctrl+K` 改绑仪表盘；②首版范围 = **统计卡 + 快捷入口**，不新建数据库表。
- 新增：`src/shared/types/dashboard.ts`（`DashboardStats` / `ModuleStat` / `RecentFile` 契约）、`src/modules/common/db/statsRepository.ts`（单次扫描 `files` 表聚合文件数/字数/标签去重数/模块分布，`listRecentFiles` 取 mtime 倒序 5 条）、`src/webview/dashboard.tsx`（shadcn Card/Button/Progress，重建期间 1.5s 轮询收敛）。
- 协议变更：新增 `dashboard/stats`（聚合快照）与 `host/command`（命令白名单 `ALLOWED_HOST_COMMANDS` = `zhai.clearIndex` / `zhai.openSettings`）；保留 `index/rebuild`；移除 `search/query` 与 `index/status`。检索能力（`searchFiles`、FTS5 表、`zhai.search.limit` 配置）保留，供后续搜索功能复用。
- 连带改动：`indexRepository.safeParseTags` 改为导出（statsRepository 复用）；状态栏 command 与 tooltip 改为「点击打开仪表盘」；`extension.ts`、`commands.ts`、`package.json`、`bootstrap.tsx`/`webviewHtml.ts`/`vite.config.mjs`/`placeholderViewProvider.ts` 的「搜索面板」表述同步。
- 文档：`docs/modules/common.md` 命令表更新并加一行 `Ctrl+K` 归属说明；`docs/modules/dashboard.md` 新增第 13 节「插件内 Webview 首版实现」，用表映射 HTTP API → 协议、StatCardRow/QuickActions/模块分布落地情况，并注明 ActivityFeed/WidgetTodo/WidgetShortcut 待实现。
- 验证：`pnpm run lint` 0 问题（修掉 import-x/order 5 处、箭头函数返回类型、`busy` → `isBusy` 布尔前缀命名）；`pnpm run compile`（typecheck + esbuild + vite 多入口）通过。
- 遗留：视图 ID 已变更，用户已保存的侧栏布局位置需重新拖一次。
- 提交（用户指令「提交到git」）：`80009b0 feat(dashboard): 将全局搜索视图改造为仪表盘`（26 文件，含 git 自动识别的 3 处重命名：searchPanelProvider→dashboardProvider、index.css→globals.css、vite-env.d.ts→src/types/）；`80f89f5 chore(memory): 记录本轮仪表盘改造与项目长期约定`。工作区已干净。
- 环境坑（重要）：命令行直接传中文参数会被 PowerShell 按本地代码页解码成乱码（实测 `@('中文'...)` 抛 ParserError）。提交信息必须先用工具写入 UTF-8 文件（无 BOM）再 `git commit -F msg.txt`，提交后删除该文件。

## 工作区视图改造为 AI 对话（用户：工作区修改为aichat）
- 用户确认三点：①**全部改名 + 加快捷键**；②首版 = **对话闭环可用**（A6/A8/A9/A11、AC-6/AC-9）；③按 ai-chat.md 走 **openai SDK**（未采用原生 fetch 方案）。
- 依赖：`pnpm add openai` → 7.15.0 进 dependencies（安装约 1 分 40 秒，链接正常）。
- 改名：视图 `zhai.placeholderView`「工作区」→ `zhai.aiChat`「AI 对话」；`common/placeholderViewProvider.ts` → `modules/ai/aiChatProvider.ts`（类 `AiChatViewProvider`，viewId 常量 `zhai.aiChat`）；webview 入口 `workspace.html`/`workspace.tsx` → `aichat.html`/`aichat.tsx`；vite 多入口键 `workspace` → `aichat`。
- 新增命令：`zhai.openAiChat`（绑 `Ctrl+Shift+L`）、`zhai.ai.setApiKey`（输入框写 SecretStorage 的 `zhai.deepseek.apiKey`）、`zhai.ai.clearApiKey`。
- 新增宿主模块 `src/modules/ai/`：`db/connection.ts`（ai.db）、`db/schema.ts`（conversations/messages/ai_usage_logs 三表 + 索引 + meta）、`db/chatRepository.ts`、`client.ts`（openai SDK + `baseURL: https://api.deepseek.com` + `reasoning_content` 收窄读取 + usage 透传）、`chatService.ts`（会话编排、流式 60ms 合并、AbortController 取消、预算护栏、30s 首字超时、按字符÷1.6 估 token）、`aiChatProvider.ts`、`commands.ts`。
- 契约与协议：`src/shared/types/aiChat.ts`（AiConversation/AiMessage/AiSessionSnapshot/AiRuntimeInfo/AiSendResult）；方法 `ai/session`、`ai/newSession`、`ai/runtime`、`ai/send`、`ai/abort`、`host/command`；宿主 `stream` 推送复用了既有 `StreamMessage`（本次扩展 `channel`/`status`/`content`/`error` 字段）。
- 前端：`src/webview/ai-chat.tsx`（消息气泡、Enter 发送 / Shift+Enter 换行、停止、新建会话、无 Key 引导卡、按内容长度自动滚底）；`bridge.ts` 新增 `onStream` / `onState` 订阅分发（原先只处理 response）。
- 顺带重构：新增 `common/db/openDatabase.ts`（`openSqliteDatabase(uri, fileName)` 基座，index.db 与 ai.db 共用 PRAGMA；`connection.ts` 改为委托）；新增 `common/webviewCommands.ts` 共享命令白名单（`WEBVIEW_ALLOWED_COMMANDS`，dashboardProvider 改为引用它，删掉本地 Set）。
- 文档：`docs/modules/ai-chat.md` 状态改「对话模式已落地」+ 功能表 A6/A8/A9/A11 标已落地（A7/A10 部分）+ 新增第 10 节（协议表、实现位置、与设计文档的差异如 tiktoken 未接入、estimated_cost 记 0、重试未实现）；`docs/modules/common.md` 命令表新增 3 条。
- 验证：typecheck 0 错误、`eslint src` 0 问题（修掉 `streamingMessageId`/`scrollAnchor` 两个 ref 命名警告 → 加 `Ref` 后缀）、`pnpm run compile` 通过，产物含 `dist/webview/index.html` 与 `aichat.html`。
- 未提交（沿用「不擅自提交」约定）；未改动 `docs/development.md` 的计划状态列。
- 用户自测前置条件：需先运行「设置 DeepSeek API Key」命令写入密钥，才能实际对话。

## AI 对话页改用 message-scroller 组件（用户：使用message-scroller组件进行构造）
- 组件来源：`@shadcn/react` 的 `message-scroller`（`src/components/ui/message-scroller.tsx` 为其封装），配套 `message.tsx`（Message/MessageAvatar/MessageHeader/MessageContent/MessageFooter）、`bubble.tsx`（Bubble/BubbleGroup/BubbleContent/BubbleReactions）。
- 关键 API：`MessageScroller.Provider`（`autoScroll`、`defaultScrollPosition: 'start' | 'end' | 'last-anchor'`、`scrollEdgeThreshold`、`scrollPreviousItemPeek`、`scrollMargin`）、`Root`/`Viewport`/`Content`/`Item`（`messageId`、`scrollAnchor`）/`Button`（默认自带 lucide ArrowDown 与自动显隐），hooks `useMessageScroller`（scrollToEnd/scrollToMessage/scrollToStart）、`useMessageScrollerScrollable`、`useMessageScrollerVisibility`。
- 重构 `src/webview/ai-chat.tsx`：删掉 `ScrollArea` 与手写 `scrollTop = scrollHeight` 滚底逻辑，改为 `Provider(defaultScrollPosition="end")` + `Viewport` + `Content` + 每个消息一个 `Item(messageId, scrollAnchor=末条)` + `Button()`；气泡由自绘 div 改为 `Message align` + `MessageAvatar/Header/Content/Footer` + `Bubble(variant: default|muted)` + `BubbleContent(whitespace-pre-wrap)`；对齐由 `align` 联动（`data-[align=end]:flex-row-reverse`、Bubble 的 `group-data-[align=end]/message:self-end`），无需手写 justify。
- 注意：`message-scroller.tsx` 头注释明确「滚动位置由组件内部托管，调用方不要同时操作容器的 scrollTop」，故不要再恢复手动滚底。
- 验证：typecheck 0 错误、`eslint src` 0 问题、`vite build` 通过（aichat 分包由 25.17 kB 增至 33.05 kB，因引入这三组组件）。
- 提交（用户指令「提交到git」）：`5e84f57 feat(ai-chat): 将工作区视图改造为 AI 对话`（27 文件，+1559/-91；git 识别 `workspace.html → aichat.html` 为重命名 70%，`workspace.tsx → aichat.tsx` 记为删除 + 新增）、`d181136 chore(memory): 记录 AI 对话改造与 message-scroller 重构`。工作区已干净。
- 本次会话共产生 4 个提交：80009b0（仪表盘）、80f89f5、5e84f57（AI 对话）、d181136。

## Bug 修复：Webview 完全没有样式（用户报「没有应用到样式」）
- 现象：仪表盘与 AI 对话页的 shadcn 组件渲染成无样式裸元素（按钮无底色、卡片无圆角/内距、message-scroller 布局错乱）。
- 定位：Tailwind 自动源检测以 Vite root（`src/webview`）为界，`src/components`、`src/hooks`、`src/lib` 位于 root 之外 → 这些目录的类名从未进入产物。证据：旧产物 `dist/webview/assets/globals-*.css` 中 `card-spacing` / `ring-foreground` / `bubble-content` / `message-footer` 等组件专属类名全部为 False，CSS 仅 16.33 kB。
- 修复：`src/webview/globals.css` 在 @import 之后新增 `@source '..';`（相对该 CSS 文件解析，等价于 `src` 目录）。重建后 CSS 16.33 kB → **190.43 kB**（gzip 27.70 kB），上述类名全部变为 True。
- 注意：`@source` 指令不可删除；`shadcn/dist/tailwind.css` 提供 `@utility scroll-fade-*` / `shimmer-*` 等（Tailwind 4 的 @utility 按需生成，必须被扫描到才输出）；`message-scroller.tsx` 用到的 `scrollbar-thin` / `scrollbar-gutter-stable` / `scrollbar-track-transparent` 在 Tailwind 与 shadcn CSS 中均无定义，属无效类（仅滚动条美化缺失，不影响布局）。
- 验证：prettier 通过、`pnpm run compile` 通过；为预览临时启动的 vite dev server（5173）已停止清理。webview 是构建产物，改样式后需重新 compile 并在扩展宿主中重开视图。

## AI 对话展示修复（用户反馈：无 markdown 渲染、头像与气泡样式不正确）
- Markdown 渲染：原先助手回复按纯文本输出（用户截图里 `**炮镇海城楼**` 原样带星号）。按 `docs/modules/markdown-one.md` 第 5 节的 unified/remark 选型，webview 侧引入 `react-markdown@10.1.0` + `remark-gfm@4.0.1`（devDependencies，随 webview 打包）。
- `src/webview/ai-chat.tsx` 新增 `MessageBody`：用户输入保持纯文本（BubbleContent 加 `whitespace-pre-wrap`），助手回复走 `<Markdown remarkPlugins={[remarkGfm]} components={MARKDOWN_COMPONENTS}>`。`MARKDOWN_COMPONENTS` 逐标签映射主题类名（p / strong / em / ul / ol / li / h1~h3 / blockquote / a / code / pre / hr / table / th / td），**不引入 typography 插件**，避免 prose 样式与气泡样式互相覆盖；code 通过 `className` 是否含 `language-` 区分行内与代码块。
- 头像修复：`MessageAvatar` 内置只有 `min-w-8` 无高度约束 → 渲染成 32×16 椭圆、图标仅 16px。改为 `className="size-8 self-start group-has-data-[slot=message-footer]/message:translate-y-0"`：正方形圆形头像 + 与消息首行对齐（组件默认 `self-end` 会把头像贴到长气泡底部，用户认为不正确），并中和组件内置的 footer 位移（有 footer 时头像会上移 2rem）。
- 验证：node 侧 `renderToStaticMarkup` 实测输出 `<strong>` / `<ul>` / `<table>` / `<code>` 均正确；eslint 0 问题、`pnpm run compile` 通过；aichat 分包 33 kB → 189 kB（未压缩，含 micromark 全家桶），仅该入口受影响。
- 未提交。用户需重载扩展宿主后查看。

### 追加修复：用户气泡宽度塌陷（用户截图反馈「用户对话气泡似乎没有完全修改好」）
- 现象：短用户消息（如「测试」）气泡宽度塌陷到最小内容宽度 → 汉字逐字竖排；长消息也只按每行 5~6 字换行。
- 根因：`Bubble` 的 `w-fit`（fit-content）与 `BubbleContent` 内置的 `max-w-full`（百分比宽度）互相依赖 —— 百分比宽度在 intrinsic 尺寸计算中没有确定基准，浏览器回退到 min-content。
- 修复：`BubbleGroup` 加 `w-full`（为 `Bubble` 的 `max-w-[80%]` 提供确定基准）；`BubbleContent` 改为 `w-max max-w-full`（短内容按 max-content 单行展示，长内容仍被 max-w 约束后换行），用户消息保留 `whitespace-pre-wrap`。
- 验证方法（可复用经验，重要）：浏览器预览下 ai 协议请求会 reject，页面拿不到消息数据，因此**新建临时探针页** `src/webview/_probe.html` + `_probe.tsx`（复制真实 Message/Bubble 结构、硬编码 4 条消息、320px 与 646px 两种容器宽度），`pnpm run webview:serve` + `agent-browser open/screenshot` 实测：短/长用户气泡均单行 ✓、助手 markdown 加粗与列表 ✓、头像 32×32 圆形并与首行对齐 ✓。确认后删除探针文件、停止 5173 端口进程、执行 `agent-browser close`。
- 注意：探针页未传 `components` 映射时列表无圆点（Tailwind preflight 清空 list-style），生产代码里 `ul` 映射了 `list-disc pl-4`，故线上有圆点。
- 验证：eslint 0 问题、`pnpm run compile` 通过（CSS 191.29 kB、aichat 189.19 kB）。未提交。

## 按 message-scroller 官方界面模式重构 AI 对话页（用户反馈「似乎没有采用 message-scroller 组件进行编写」）
- 澄清：MessageScroller 组件一直在用（Provider / Viewport / Content / Item / Button），但**空态与输入区此前是自绘 markup**，与官方 demo 的界面模式不符 —— 这是用户看到差异的原因。
- 加载 shadcn skill（`use_skill("shadcn")`）后按其强制规则改造：Empty states 用 `Empty`、输入区含按钮用 `InputGroup` + `InputGroupTextarea`/`InputGroupButton`、分区用 `Separator`（替代 `border-t`/`border-b`）、条件类名走 `cn()`、Button 内图标用 `data-icon` 且不加 `size-*`。注意 `pnpm dlx shadcn@latest docs message-scroller message bubble` 对这三个组件**无文档链接**（它们由 `@shadcn/react` 包提供，不在 registry 文档索引内）。
- 改动（`src/webview/ai-chat.tsx`）：
  - header：标题 + 副标题（模型 / 今日 token）+ 右侧圆形 `SquarePen` 新建会话按钮，下方 `Separator`。
  - 空态三种：`WelcomeState`（`Empty` + `EmptyMedia variant="icon"` 放大为 `size-12 rounded-2xl` + `EmptyTitle` + `EmptyDescription`）、`ApiKeyState`（原 Card 形态的 `ApiKeyGuide` 改为 `Empty` + `EmptyContent` 内按钮）、`LoadingState`；运行时信息未到位时先显示 Loading，不再误报「无 Key」。
  - 输入区：裸 `Textarea` → `InputGroup` + `InputGroupTextarea` + `InputGroupAddon align="block-end"`（左：插件设置按钮；右：发送 / 停止，均为 `InputGroupButton size="icon-sm" className="rounded-full"`，发送用 `variant="default"` + `ArrowUp`），底部说明文字居中，错误时切 `text-destructive`。
  - 「正在准备上下文…」改用 `Marker` + `MarkerIcon` + `MarkerContent`。
- 验证：eslint 0 问题、`pnpm run compile` 通过（aichat 分包 196.96 kB，CSS 191.42 kB）。本轮未做浏览器截图（预览命令被跳过），依据为官方 demo 结构与 skill 规则；探针文件与 5173 预览进程已清理。
- 未提交：本轮重构 + 之前几轮的展示修复（globals.css 的 `@source`、markdown 渲染、头像尺寸、气泡宽度）都在工作区。另注：`.vscode/launch.json` 也存在未提交改动，但非本次会话所改。

## 待确认方案：shadcn 暗色令牌与 VSCode 主题同步（用户要求「先给出方案看看」）
- 现状：`src/webview/globals.css` 的 `:root`（亮）与 `.dark`（暗）都是**硬编码 oklch**（如 `.dark` 的 `--background: oklch(0.145 0 0)`、`--primary: oklch(0.45 0.085 224.283)` 青色系）；`bootstrap.tsx` 只按 `data-vscode-theme-kind` 挂 `.dark` 类，颜色与用户的 VSCode 主题无关。
- 方案核心：Webview 内 `--vscode-*` 变量由 VSCode 注入且**随主题实时变化**，把 shadcn 语义令牌改为引用这些变量即可自动跟随（亮/暗/高对比、任意第三方主题），无需 JS 参与。
- 推荐映射：`--background/--foreground` ← `--vscode-sideBar-background/-foreground`；`--card/--popover` ← `--vscode-editorWidget-background/-foreground`；`--primary(-foreground)` ← `--vscode-button-background/-foreground`；`--secondary*` ← `--vscode-button-secondary*`；`--muted` ← `--vscode-input-background` 或 `--vscode-list-hoverBackground`；`--muted-foreground` ← `--vscode-descriptionForeground`；`--accent` ← `--vscode-list-hoverBackground`；`--border` ← `--vscode-panel-border`；`--input` ← `--vscode-input-border`；`--ring` ← `--vscode-focusBorder`；`--destructive` ← `--vscode-errorForeground`；`--chart-1..5` ← `--vscode-charts-*`；字体可选 ← `--vscode-font-family`。所有 var() 都要带 fallback（部分主题不设 `sideBar-border`/`input-border` 等）。
- 两种落地形态待用户选：①**仅同步暗色**（只改 `.dark` 块，`:root` 的 shadcn 亮色保留，改动最小、最贴合原话）；②**全跟随 VSCode**（`:root`/`.dark` 都映射，可直接删除 `.dark` 的颜色块，亮暗一致）。
- 已知影响：主按钮会从青色变为主题的 `button.background`（多为蓝）；`.dark` 类同步需保留（`dark:` 变体仍依赖它）；`bubble.tsx`/`button.tsx` 里的 `color-mix(in oklch, var(--primary), …)` 与 `oklch(from var(--primary) …)` 对 hex/rgb 输入同样有效，不受影响；圆角 `--radius` 无对应 VSCode 变量，保持不动。涉及文件仅 `src/webview/globals.css`（+ 可选 `bootstrap.tsx`）。
- 状态：方案已提交用户评审，尚未实施。
- 我方建议（2026-09-14 追加）：**形态 B（亮暗都跟随 VSCode）**——心智模型最简单（webview 即 VSCode 一部分）、代码最少、切任意主题/高对比都协调；**主色跟随**（`--primary` ← `--vscode-button-background`，青色成为历史）；`--muted` ← `--vscode-list-hoverBackground`（hover 与气泡同色，符合 muted「比背景略突出」语义）；**字体本轮不动**；映射块写在 `globals.css` **末尾**（不能用 `@import` 引入独立文件——CSS @import 插到顶部会先被后面的 `:root`/`.dark` 硬编码值覆盖），并加注释「shadcn preset 覆盖后需补回这段」。

### 已实施：令牌桥接 VSCode 主题（用户回复「开始」）
- 按上述建议全部落地（形态 B + 主色跟随 + muted 用 hoverBackground + 字体不动）。
- `src/webview/globals.css`：**删除**原 `:root`（亮）与 `.dark`（暗）两套硬编码 oklch 色值，改为文件末尾的 `:root, .dark { … }` 桥接块（27 个令牌，全部写成 `var(--vscode-*, fallback)`）；`@source '..'`、`@theme inline`、`@layer base` 原样保留；`--radius` 保持 `0.625rem`（VSCode 无圆角变量）。
- 映射要点：`--background/--foreground` ← `sideBar-background/-foreground`；`--card/--popover` ← `editorWidget-background/-foreground`；`--primary(-foreground)` ← `button-background/-foreground`（青色主按钮自此跟随主题）；`--secondary*` ← `button-secondary*`；`--muted`/`--accent` ← `list-hoverBackground`；`--muted-foreground` ← `descriptionForeground`；`--accent-foreground` ← `list-activeSelectionForeground`；`--border` ← `panel-border`；`--input` ← `input-border`；`--ring`/`--sidebar-ring` ← `focusBorder`；`--destructive` ← `errorForeground`；`--chart-1..5` ← `charts-*`；`--sidebar*` ← 侧栏与 activityBar 变量。
- 回退值（浏览器预览无 `--vscode-*` 时）取 VSCode 暗色默认：`#1f1f1f` / `#cccccc` / `#0078d4` / `rgb(255 255 255 / 8%)` 等。
- 验证：prettier 通过、eslint 0 问题、`pnpm run compile` 通过；产物 CSS 中 `--vscode-sideBar-background`、`--vscode-button-background`、`--vscode-list-hoverBackground`、`--vscode-focusBorder`、`--vscode-panel-border` 全部存在（True），确认引用未被构建期求值。
- 维护提示已写入 CSS 注释：`shadcn apply / init` 会重写 globals.css，覆盖后需补回桥接块。
- 未提交；需重载扩展宿主查看效果。`.dark` 类同步（bootstrap.tsx）保留，仍供 `dark:*` 变体使用。

## 可继续与 VSCode 同步的样式清单（用户问「还有哪些样式可以与vscode同步」）
- 已同步：颜色语义令牌（27 项，见上）。
- 高价值待同步（未实施）：
  1. **滚动条** ← `--vscode-scrollbarSlider-background/-hoverBackground/-activeBackground`。现状最明显：`message-scroller.tsx` 引用的 `scrollbar-thin` / `scrollbar-gutter-stable` / `scrollbar-thumb-transparent` 在 Tailwind 与 shadcn CSS 中**都不存在**，所以用的是 Chromium 默认滚动条（暗色下很突兀）。需自建 `::-webkit-scrollbar` 规则或补自定义 utility。
  2. **字体族 / 字号 / 字重** ← `--vscode-font-family`、`--vscode-font-size`（默认 13px，当前页面用 text-sm=14px）、`--vscode-font-weight`；代码块另取 `--vscode-editor-font-family` / `-font-size` / `-font-weight`。同步后可**移除 `@fontsource-variable/inter` 依赖**（产物少约 200 kB 字体）。
  3. **Markdown 片段** ← `--vscode-textCodeBlock-background`（替换 `pre`/`code` 现在的 `bg-background/60`）、`--vscode-textBlockQuote-background` / `-border`、链接 `--vscode-textLink-foreground`。
  4. **输入控件** ← `--vscode-input-background`（当前走 `dark:bg-input/30` 派生）、`--vscode-input-placeholderForeground`（替换 `placeholder:text-muted-foreground`）、按钮 hover 用 `--vscode-button-hoverBackground` / `-secondaryHoverBackground` 替代 `color-mix` 派生。
  5. **选中与焦点** ← `::selection` 用 `--vscode-editor-selectionBackground/-foreground`；高对比主题下用 `--vscode-contrastBorder` / `-contrastActiveBorder` 补实边框（可用 body 上的 `vscode-high-contrast` 类判断）。
  6. **阴影** ← `--vscode-widget-shadow`（替换 shadcn 硬编码 shadow）。
  7. **动效** ← `--vscode-*` 无对应变量，但可加 `@media (prefers-reduced-motion: reduce)` 尊重系统「减少动效」。
- 无法同步：圆角（VSCode 无 CSS 变量）、图标体系（codicon vs lucide）、间距密度（无变量）。
- 状态：清单已提交用户选择，尚未实施。

### 已实施：第二批 VSCode 同步（用户回复「全部按建议实施」）
- 6 项全部落地：
  1. **滚动条**：`globals.css` 新增 `::-webkit-scrollbar` 全套（track / thumb / hover / active / corner），颜色取 `--vscode-scrollbarSlider-*`；新增 `[data-autoscrolling]::-webkit-scrollbar-thumb { background-color: transparent }` 顶替 `message-scroller.tsx` 里那两个未定义的 `data-autoscrolling:scrollbar-*` 类，并删掉无效的 `scrollbar-thin` / `scrollbar-gutter-stable`。
  2. **字体**：`@theme inline` 的 `--font-sans` → `var(--font-ui)`（桥接 `--vscode-font-family`），`--font-mono` → `var(--font-code)`（桥接 `--vscode-editor-font-family`）；**卸载 `@fontsource-variable/inter`** 并删除其 `@import`（产物 woff2 归零）。
  3. **Markdown 片段**：新增 `--code-background`（`textCodeBlock-background`）、`--quote-background`、`--quote-border`、`--link`；`ai-chat.tsx` 的 pre/code 由 `bg-background/60` 改用 `bg-code-background`，blockquote 加底色与专用边框，链接加 `text-link`。
  4. **输入控件**：新增 `--placeholder`（`input-placeholderForeground`）、`--input-background`（`input-background`）、`--primary-hover`（`button-hoverBackground`）；`input.tsx` / `textarea.tsx` 的 placeholder 类改 `text-placeholder`，`input-group.tsx` 的 `dark:bg-input/30` 改 `bg-input-background`，`button.tsx` default 变体 hover 改 `bg-primary-hover`。
  5. **选中与高对比**：`::selection` 取 `--vscode-editor-selectionBackground/-Foreground`；`body.vscode-high-contrast(-light)` 下把 `--border` / `--input` / `--ring` 指向 `--vscode-contrastBorder` / `contrastActiveBorder`。
  6. **阴影与动效**：新增 `--widget-shadow`（`--vscode-widget-shadow`，经 `@theme inline` 暴露为 `shadow-widget` 供后续弹层使用）；新增 `@media (prefers-reduced-motion: reduce)` 全局降级。
- 未同步（技术上不可行）：圆角（VSCode 无变量）、图标体系（codicon vs lucide）、间距密度；**字号保持 shadcn 的 16px 基准**（若跟随 VSCode 的 13px，因组件大量使用 `text-sm`/`text-xs` 绝对 rem，会整体缩小约 19% 并破坏排版层级）。
- 验证：prettier ✓、eslint 0 问题 ✓、`pnpm run compile` ✓；产物 CSS 中 `bg-code-background` / `bg-input-background` / `text-placeholder` / `primary-hover` / `bg-quote-background` / `text-link` / `--font-ui` / `scrollbarSlider` / `vscode-font-family` / `vscode-editor-selectionBackground` 全部存在；woff2 字体文件 0 个；CSS 由 191.42 kB 降至 188.63 kB（gzip 26.93 kB）。
- 改动文件：`src/webview/globals.css`、`src/webview/ai-chat.tsx`、`src/components/ui/{input,textarea,input-group,button,message-scroller}.tsx`。后 5 个属 shadcn 托管目录：`shadcn apply / init` 覆盖时会丢失这些类改动，需重新应用（globals.css 的桥接段同理）。
- 未提交；需重载扩展宿主查看。

## package.json 整理（用户：@package.json 整理文件）
- 依赖：删除未被任何源码引用的 `date-fns`；`docs/modules/dashboard.md` 第 9 节的依赖清单表同步修正（原表还错列了未安装的 `@tabler/icons-react` 与 `react-router-dom`，已改为实际的 `lucide-react`、`react-day-picker`，未安装项标为 ❌）；`dependencies` 仍为 `better-sqlite3` + `openai`。
- 版本对齐：`engines.vscode` 由 `^1.90.0` 提升到 `^1.137.0`，与 `@types/vscode@^1.137.0` 一致（消除 vsce 警告）。
- 结构整理：`scripts` 按「构建 → webview → 校验 → 格式 → 打包」重排（11 项，功能不变）；`contributes.commands` 按「面板跳转 → 索引 → AI 密钥 → 设置 / 诊断」重排（9 项）；顺带删除仓库根目录的遗留日志 `add2.log`。
- **失误与纠正**：`.vscodeignore` 原本已存在，我因 `list_dir` 不显示 dot-files 而误判为缺失，用 write_to_file 覆盖了它。核对 `git diff` 后原规则全部保留，仅新增 `.codebuddy/**`、`eslint.config.mjs`、`components.json`、`pnpm-*.yaml`、`.editorconfig`、`.prettierrc*`、`*.log`、`*.tsbuildinfo`，并把过时的 `vite.webview.config.ts` 更正为 `vite.config.mjs`，同时补回漏掉的 `node_modules/**`。
- 待用户决策：`.vscodeignore` 的 `node_modules/**` 会让 vsix 缺少 `better-sqlite3`（原生模块）与 `openai` 运行时依赖，与扩展惯例相反；已按原意保留并在文件内加注释提醒。
- 其他建议（未实施）：`package` 脚本引用的 `vsce` 未在 devDependencies 声明（依赖全局安装）；`@types/node` 未显式声明（靠间接依赖）；`recharts` 锁定精确版本 `3.10.1` 而其余用 `^`；缺 `repository` / `homepage` / `keywords` / `icon` 等发布元信息。
- 验证：`node -e` 结构校验（dependencies 2、devDeps 44、无 date-fns、engines ^1.137.0、scripts 11、commands 9）✓；prettier ✓；`pnpm run compile` ✓。
- 教训：写文件前先确认目标是否存在——`list_dir` 不列出 dot-files，覆盖用户既有配置会造成静默丢失。

### 追加：补齐打包工具与发布元信息（用户回复「需要」）
- 新增 devDependencies：`@vscode/vsce@^3.9.2`（原 `package` 脚本依赖全局 vsce，未声明）、`@types/node@^26.5.1`（此前靠间接依赖）。
- `pnpm-workspace.yaml` 的 `allowBuilds`：安装 vsce 时 pnpm 自动写入两行占位（`'@vscode/vsce-sign': set this to true or false`、`keytar: set this to true or false`），触发 `ERR_PNPM_IGNORED_BUILDS` —— 与 9-14 早前记录的同类坑一致。决策：`@vscode/vsce-sign: true`（打包/签名所需原生绑定）、`keytar: false`（已弃用原生模块，Node 26 下无法编译，仅用于 vsce 登录态存储）。改完 `pnpm install` 恢复正常。
- **vsce + pnpm 冲突（重要）**：`vsce package` 内部用 `npm list` 检测生产依赖，pnpm 的符号链接布局会让它报出大量 `npm error missing: tsd@… / xo@… / dunder-proto …` 并中止，不产出 vsix。解法：`package` 脚本改为 `vsce package --no-dependencies`（与 `.vscodeignore` 已忽略 `node_modules/**` 的策略一致）。
- 实测打包成功：`zhai-vscode-0.1.0.vsix`，**12 文件 / 316.81 KB**，内容仅 `package.json`、`readme.md`、`dist/**`、`resources/icon.svg` —— src / docs / .codebuddy / 构建配置 / node_modules 全部被排除，`.vscodeignore` 生效；vsix 已被 `.gitignore` 的 `*.vsix` 覆盖，不会入库。
- 元信息：补 `repository`（取自 git remote `https://github.com/baiwanyi/zhai.vscode.git`）、`homepage`、`bugs`、`keywords`（8 个英文词）；`recharts` 由精确 `3.10.1` 改为 `^3.10.1`。
- 未加：`icon` 字段（vsce 要求 PNG ≥128×128，仓库只有 `resources/icon.svg`，svg 仍用于视图图标）；`files` 字段（与 `.vscodeignore` 二选一，已有后者）。
- **遗留风险**：当前 vsix 不含 `better-sqlite3`（原生模块）与 `openai`，装到 VSCode 后插件无法运行；本地 F5 调试不受影响（走工作区 node_modules）。发布前需定策略：去掉 `.vscodeignore` 的 `node_modules/**` 并让 vsce 正常携带生产依赖，或用 esbuild 把依赖打进入口（better-sqlite3 需 `--external` 并随包提供 prebuild）。
- 验证：prettier ✓、`pnpm run compile` ✓、`vsce package --no-dependencies` ✓。

## .vscode 调试配置整理（用户：@.vscode/launch.json 修改合适的文档）
- 检查结论：`.vscode/` 下有 launch.json / tasks.json / settings.json。launch.json 此前只有单个「运行扩展」配置且无任何说明；tasks.json 缩进是 2 空格（与项目 4 空格 + Prettier 不一致）；settings.json（formatOnSave + Prettier）无需改动。
- `launch.json`：加 JSONC 注释说明「F5 启动扩展宿主 / 宿主产物 dist/extension.js 与 webview 产物 dist/webview / webview 是构建产物，改前端后需重新构建并在宿主内重开视图」；原配置补 `sourceMaps: true` 与 `skipFiles: ["<node_internals>/**"]`；新增「运行扩展（跳过编译）」配置，配合常驻 watch 使用，避免每次 F5 都跑全量 compile（typecheck + esbuild + vite 约 10 秒）。
- `tasks.json`：缩进统一 4 空格；保留 `compile`（`problemMatcher: "$tsc"`，仍是 `preLaunchTask` 的目标，名称与 launch.json 一致）；新增 `watch:all`（isBackground）与 `webview 预览`（isBackground），后者注释说明浏览器内没有 `--vscode-*` 变量也没有宿主通信，仅用于核对样式布局。
- 验证：`prettier --check` 三个文件全绿（Prettier 的 jsonc 解析器会保留注释）。改动量 launch.json +42/-15、tasks.json +46/-13。
- 依赖携带策略：已实施，见下方「发布依赖策略落地」一节（原「仍待用户决定」作废）。

## 发布依赖策略落地 + 四个提交（用户回复「1、2、都需要」）
- 事实澄清：esbuild 已 `bundle: true`，`openai` 早就打进 dist/extension.js，真正需要随包的只有 `better-sqlite3`（唯一 external）。
- 失败路径记录（避免重走）：想用 pnpm 的 hoisted 布局让 vsce 的 `npm list` 可用 → ① 写在 `.npmrc` 无效（pnpm 11+ 项目级配置在 pnpm-workspace.yaml）；② 写在 pnpm-workspace.yaml 必须 camelCase，写成 `node-linker` 会被**静默忽略**（pnpm 只给一条 WARN）；③ 改成 `nodeLinker: hoisted` 后需删除 node_modules，但环境有 safe-delete 保护、pnpm 又报 `ERR_PNPM_PACKAGE_MANAGER_REMOVE_MODULES_DIR` → 放弃并回滚配置（文件内留注释说明键名必须 camelCase）。
- **最终方案**：保留 `vsce package --no-dependencies`，新增根目录 `bundle-native.mjs`：先写 `dist/package.json`，再用 npm 在 dist/ 下装生产依赖（`--omit=dev`）；Node 从 dist/extension.js 向上查找即可解析 `dist/node_modules/better-sqlite3`。脚本还校验 `prebuilds/<platform>-<arch>.node` 存在，缺失即抛错，避免产出坏包。
  - 要点：better-sqlite3 v13 的原生二进制在 `prebuilds/`（不是 `build/Release/`）且基于 N-API，跨 Node / Electron ABI 稳定，无需重建；Windows 下必须经 shell 调 npm（Node 20+ 禁止直接 spawn `.cmd`，否则 `spawnSync npm.cmd EINVAL`）。
  - `.vscodeignore`：`node_modules/**` 改为 `/node_modules/**`（锚定根）以放行 dist 内依赖；另忽略 dist 内依赖的 src / deps / *.gyp / *.md 与锁文件。
- 实测：`zhai-vscode-0.1.0.vsix` **72 文件 / 8.74 MB**，内容含 `better-sqlite3/ (45 files)`（8 个平台 prebuilds，未压缩 16.19 MB）。
- 插曲：`pnpm install --force` 中途失败导致 node_modules 不一致，`node_modules/.bin/vsce` 丢失（报 `Command "vsce" not found`），重跑 `--force`（7m55s）后恢复。
- 提交（用户指令「1、2、都需要」）：`fd5cf04 fix(webview): 修复样式缺失、补 Markdown 渲染并将主题同步到 VSCode`、`031e3bf chore(package): 整理 package.json 并打通打包与发布链路`、`581624f chore(vscode): 完善调试配置说明与常用任务`、`82fa3a3 chore(memory): 记录主题同步、打包链路与整理工作`。工作区已干净。
- 环境提示：`Remove-Item` 删除操作（尤其批量）会触发 safe-delete 保护或审批超时，优先用 delete_file 工具或避免在命令里混入删除；`pnpm run package`（含 compile + npm install）易被判定为长任务而中断，打包应拆成 `node bundle-native.mjs` 与 `pnpm exec vsce package --no-dependencies` 两步执行。
- 临时文件清理（用户指令「删除临时文件」）：根目录 `msg1~5.txt`（提交信息临时文件）、`zhai-vscode-0.1.0.vsix`（打包产物）与全部过程日志（`pk*.log` / `bn*.log` / `i5.log` 等）已删除。工作区 `git status` 干净。
- 补充结论：删除操作在**用户在线并明确授权**时（如「删除临时文件」）可正常执行，此前失败均为审批提示超时；批量删除仍可能被 safe-delete 的 bulk guard 拦下（`node_modules` 那次），此时改用 `pnpm install --force` 等包管理器自身能力。

## 新增「添加到宅对话」（编辑器右键加入 AI 上下文）
- 需求：编辑器右键把选中文本（取所在行范围，标签如 `xxx.md:11-19`）或整篇（`xxx.md`）加入 AI 对话上下文，并给出添加提示。用户要求先出方案（对照 CodeBuddy / Copilot / Cursor 的 attach context 做法）再实施，随后回复「按建议实施」，按推荐项全部落地。
- 关键决策：**惰性引用**（添加只记路径 + 行范围，发送时才读内容，内存态文档优先）；历史中仅最近一条带引用的消息注入原文、更早的只留引用说明；refs 落库 `messages.refs`；添加后默认聚焦面板 + 状态栏短提示（新增 `zhai.ai.autoFocusOnContext`）；单命令固定文案；不限文件类型；`zhai.ai.maxContextTokens` 默认 4000 → 8000。
- 新增 `src/modules/ai/contextRefs.ts`：`createContextRefs`（选区 → 1-based 行范围；末光标停在行首且跨行时不计入该行）、`resolveContextRefs`（内存文档 → 磁盘回退，失效返回 null）、`mergeContextRefs`（同文件同行范围去重）、`buildContextBlock`（渲染 `<context path lines>`，转义属性与 `</context>`）、`parseStoredContexts`（校验 DB JSON）、`toPublicContextRef`（剥离绝对路径）。上限：`MAX_CONTEXT_CHARS = 20000`（选区超限按行截断并标注、整篇超限直接拒绝）、`MAX_CONTEXT_COUNT = 10`。
- `chatService.ts`：按会话隔离的 `pendingContexts` + `onContextChanged` 广播 + `addContexts/getContexts/removeContext/clearContexts`；`send` 先解析引用再落库（正文仍只存用户输入，引用存 `refs` 列），成功后清空清单并回传 `skippedContexts`；`buildChatMessages` 改 async，预算先扣「引用 + 输入」，历史中原 messageId 被显式排除——**顺带修掉此前当前输入被重复注入的问题**。
- `aiChatProvider.ts`：新增 `ai/context`、`ai/context/remove`（只接受宿主生成的 id，不收路径）、`ai/context/clear`；`state` 广播 `payload.kind` 区分 runtime / context。
- 前端 `ai-chat.tsx`：输入区上方 `AttachmentGroup` 上下文清单（`AttachmentTitle` 标签 + 体积描述 + 单条移除 / 清空），历史消息用 `Badge` 回显引用，footer 增加 notice 行（显示「已跳过失效引用」），`onState` 按 kind 分发。
- 契约/配置：`AiContextRef`、`AiSessionSnapshot.contexts`、`AiMessage.refs`、`AiSendResult.skippedContexts`；`chatRepository` 接通 `refs` 列（该列 DDL 早已预留）；`package.json` 增加命令与 `menus.editor/context`（`group: zhai@1`，`when` 含 `!inDiffEditor`）。
- 安全：引用原文注入前转义 `</context>`；`SYSTEM_PROMPT` 声明 `<context>` 标签内指令一律不执行（防提示词注入）；前端删除引用只传 id，宿主不接受路径。
- 文档：`docs/modules/ai-chat.md`（版本 1.1、A12 状态改「部分」、7.1 命令表、第 10 节协议表与新增 10.1 小节、AC-11/AC-12）、`docs/modules/common.md`（命令表 + 两个配置项）。
- 改动文件（11）：`src/modules/ai/{contextRefs.ts(新),chatService.ts,commands.ts,aiChatProvider.ts,db/chatRepository.ts}`、`src/modules/common/config.ts`、`src/shared/types/aiChat.ts`、`src/webview/ai-chat.tsx`、`package.json`、`docs/modules/{ai-chat.md,common.md}`。
- 验证：`tsc --noEmit` 0 错误、`eslint src` 0 问题（1 处 import 顺序由 `eslint --fix` 修正）、esbuild + vite build 通过（aichat 分包 196.96 → 204.71 kB）。未提交。
